# CLAUDE.md — Istruzioni per future sessioni Claude in questo repo

## Cosa è questo progetto

**HOC Pro** (nome storico repo: `hoc-fan-agent`): console operativa interna di House of Creators.
Next.js 14 (App Router, JavaScript). Stack: Clerk (auth), Vercel KV (storage), Anthropic SDK (LLM), Tailwind, SWR.

Tre moduli reali, in ordine di importanza attuale:
1. **Performance & HR** — leaderboard Sales CP (score v3 da CreatorsPro), vista creator-first, Action Center (underperformers → export HR), Coaching Center. È il riferimento per le decisioni HR sui chatter (cfr `docs/BOARD_ANNOUNCEMENT_HOC_PRO.md`, che è di fatto l'ADR della formula di scoring).
2. **Comp & Ben** — P&L Live per creator, scaglioni a confronto, comp-review (hot list anomalie), comp-exam, comp-calendar, threshold-study, payment profiles.
3. **Training Academy** — simulatore chat con fan AI (`src/app/page.js`), playbook, leghe/ladder, certificazioni. Modulo rifinito ma senza operatori onboardati (a luglio 2026 gli utenti reali sono 5-10 persone del board).

Modulo **Content Pipeline**: scaffolding non implementato (API rispondono 501). Rimosso dalla navigazione a luglio 2026; le route esistono ancora sotto `/content-pipeline` (gate `publicMetadata.contentPipeline`). Non esporlo in nav finché non è costruito.

Tutto sotto auth Clerk tranne `/sign-in` e `/sign-up` (vedi `src/middleware.js`).

## Aree del codice

- `src/app/leaderboard/` — Sales CP, creator-first (+heatmap), training ladder/leghe/storico, operational (KPI Infloww da CSV, senza voce di nav ma il drill-down `[employee]` è la scheda operatore usata da tutta l'app)
- `src/app/admin/` — hub + ~35 pagine: comp toolkit, action/coaching center, sync CreatorsPro (`creatorspro-sync`, `wage-audit`), viste Infloww (`infloww-agency`, `infloww-revenue`, `infloww-reconcile`), anagrafiche, ruoli/accessi
- `src/lib/creatorspro-api.js` — client CP v2.1 (login email/password); `src/lib/infloww-api.js` — client Infloww
- `src/lib/creator-aggregates.js` — matrix operatore×creator + costanti decisionali (SCORE_BLEND 70/30, SCORE_WEIGHTS 85/15, MIN_SHIFTS_RELIABLE=3)
- `src/lib/creatorspro-score.js` — score CP v3 (rationale versionato nei commenti)
- `src/lib/leaderboard-calc.js` / `leaderboard-config.js` / `leaderboard-history.js` — leaderboard operativa Infloww
- `src/lib/rbac.js` — capability + ruoli (vedi sotto)

## ⚠️ PRIMA di chiudere ogni PR UI

**Leggi e applica [docs/PR_PREFLIGHT.md](docs/PR_PREFLIGHT.md)**. È una checklist aggiornata con tutti i bug emersi nelle iterazioni precedenti.

Riporta nella PR description una sezione `## Self-review` con i bullet del preflight pertinenti spuntati. Se non spunti niente è perché stai dimenticando di verificare qualcosa.

## Design system

**docs/DESIGN.md è vincolante** per tutta la UI (Dark SaaS: un solo accent viola, flat, gerarchia per luminosità, sentence case, niente emoji nel chrome). Token nell'oggetto `CP` di `src/lib/brand.js`; i nomi legacy `COLORS.*` (obsidian, charcoal, champagne…) sono rimappati sui valori nuovi — non usarli in codice nuovo, usa `CP`.
Debiti noti accettati: font-weight 600/700 ancora diffusi inline (normalizzazione a 400/500 = sweep separato); `CREATOR_DOT_PALETTE` multicolore mantenuta (funzionale: distingue i creator nei dot, non è decorazione).

## Convenzioni operative

- **Branch**: `feature/<descrizione-breve>` o `fix/<descrizione>`
- **Commit**: `feat(area): cosa` / `fix(area): cosa` / `chore(deps): cosa`
- **PR**: single squash commit, mai merge commits
- **Sessioni parallele → git worktree separato**: più sessioni Claude sulla STESSA working dir condividono `.next` → un `npm run build` di una corrompe il `next dev` dell'altra (`Cannot find module './vendor-chunks/...'`), e le modifiche uncommitted si intrecciano nel working tree (entrambi visti lug 2026). Ogni sessione che **implementa** (non solo design/ricerca) lavora in un worktree isolato: `scripts/new-worktree.sh <nome> [porta]` crea `.claude/worktrees/<nome>` su un branch nuovo da `origin/main`, con `node_modules` condiviso ma `.next` e porta dev dedicate. Sincronizzarsi con le altre sessioni **prima** di toccare file condivisi (Sidebar/AppShell/hub `admin/page.js`/CLAUDE.md/`src/lib/*` di scoring). Se proprio si condivide la dir: committa isolando i tuoi hunk (staging chirurgico per-hunk / index surgery) e non buildare mentre gira il dev di un'altra sessione.
- **Git Data API**: per PR multi-file usa lo script pattern in `/tmp/single-commit-*.py` (esempi nelle sessioni precedenti) per fare 1 deploy Vercel invece di N. Vedi `docs/PR_PREFLIGHT.md` sezione 8.
- **No `await auth()` lato client**: si usa `useUser()` o SWR su `/api/whoami`. Lato server `await auth()` + `authorize(CAPABILITIES.XXX)`.
- **lucide-react è pinnata (0.395)**: prima di usare un'icona nuova verifica che esista con `node -e "console.log(!!require('lucide-react').NomeIcona)"` — icone inesistenti = React #130 in produzione.

## Auth, ruoli e capability

- RBAC custom in `src/lib/rbac.js`: capability con scope own/team/all; ruoli predefiniti operator / team_lead / sales_manager / qa_reviewer / admin + ruoli custom (`/admin/ruoli-custom`). La capability più usata nelle pagine admin è `SEED` (admin only).
- Status admin (`src/lib/admin.js`), 3 sorgenti in OR: env `HOC_ADMIN_USER_IDS`, Clerk `publicMetadata.role === "admin"`, set KV `admins:set` (gestibile da `/admin/access`).
- ⚠️ Il gating è a livello API, non di UI: le pagine `/admin/*` si aprono a chiunque sia loggato (le fetch tornano 403).
- Le API con dati denaro (`operational`, `sales-cp`, `creators[/alias]`, `employee-history`, `operator-cp-history`, `operator-drilldown`) richiedono `authorizeAll(SCORES_VIEW)` = scope "all" (admin/SM/QA; team_lead e operator → 403). Le API training/gamification (ladder, hall of fame, health, leghe, dati own) restano auth-only by design. Helper `authorizeAll` in `rbac.js` (PR #24, lug 2026).

## Navigazione

- Sidebar globale: `src/components/Sidebar.js` (array `NAV_GROUPS` + `ESSENTIAL_HREFS` per la modalità Essential)
- Hub admin: `src/app/admin/page.js` (array `SHORTCUT_GROUPS`)
- Per aggiungere una pagina: aggiorna **entrambi** (la divergenza sidebar/hub è stata una fonte di confusione storica)

## Pattern KV principali

- `ops_kpi:{period_type}:{period_id}` — dati Infloww del periodo; `ops_kpi:imports` — ZSET indice import; `ops_kpi:group_categories`, `group_languages`
- `ops_kpi:score_snapshot:{pt}:{pid}` + ZSET `ops_kpi:score_snapshots` — formula score congelata a ogni import (drift detection); `ops_kpi:score_draft:{id}` + set `ops_kpi:score_drafts:all` — bozze formula con backtest/publish
- `wages:{period_id}` e chiavi sync CP (job in KV, vedi `src/lib/` sync); mapping CP↔Infloww via `/api/admin/creatorspro-mapping`
- `infloww:txns:{period}:{creatorId}[:cN]` + `infloww:refunds:{period}:{creatorId}` + `infloww:txns:meta:{period}` — ledger transazioni fan-level per l'albero payout (`payout-ledger.js`, TTL 400gg, job `infloww:txns:job`); matching alias↔creator condiviso in `creator-match.js` (overrides in `infloww:reconcile:overrides`)
- `leaderboard:exclusions`, `underperformers:ignored`
- `employee_profile:{name}` + `employee_profile:_index` (set)
- `session:{id}`, `score_hist:*` — sessioni training e storico score
- `roles:{userId}` (multi-ruolo), `role:{userId}` (legacy), `custom_role:{id}`, `admins:set`
- `roadmap:items` — roadmap di prodotto (`/admin/roadmap`, admin-only)
- `content:*` — namespace content-pipeline (vedi `docs/CONTENT_PIPELINE.md`)
- Audit di azioni (non di uso): `audit:leaderboard-actions` (cap 200), `content:audit:log` (cap 5000). Non esiste tracking di utilizzo pagine.

## Decision log

Decisioni strutturali tracciate qui (1 riga + link); il rationale dello scoring vive in `docs/BOARD_ANNOUNCEMENT_HOC_PRO.md` e nei commenti versionati di `creatorspro-score.js` / `creator-aggregates.js`.

- **2026-07-26 — Carte-lezione Academy: curriculum estratto dal reale, non scritto a mano**: nuovo formato `src/lib/lesson-cards.js` (v `lesson-1-2026-07`) + superficie `/academy/lezioni` (+`[id]`) via API auth-only `/api/academy/lessons` (mai import client dei contenuti: i chunk statici Next sono pubblici). Ribaltamento di metodo (Nicholas): la fonte del curriculum sono le SEQUENZE REALI che hanno venduto — estrazione BigQuery (episodio = PPV stesso fan gap ≤90min; vinto = txn `message` ≤6h; mass esclusi ≥10 fan/ora), sequenze etichettate vinte-vs-perse, distillazione con critici avversariali (onestà-dati/compliance/pedagogia), curatela umana. Disciplina di grounding a 4 etichette (dato / correlazione / osservato-contrastato / osservato): il chip certifica SOLO l'enunciato. Prima carta "ppv-gradini-elisa" (21.297 episodi, 200 seq): multi-send 62,4% vs 6,1% (caveat selezione), catene-script 26W/1L, gradini-con-ragione 30/13 vs sconto-secco 10/22, upsell post-acquisto 48,8%. Generalizzazione TESTATA su 2º creator (Ottorini, 15.377 episodi): strutturale regge (multi-send 69,5/13,7, upsell 59,6), il SISTEMA no (template reuse 10,1% vs 71,8%, catene quasi assenti, sconto −20,7% vs −48,7%) → la carta separa "metodo generale" da "sistema-creator". Linea rossa a schermo: il push dopo vincolo economico reale VINCE nei numeri (62,9% vs 30,3%) ma NON si insegna (policy playbook-008 sopra profitto osservato; flag regex dichiara di mischiare obiezione/vincolo). Fuori da score/comp. Non costruito: lezione silenzio/re-hook (74% degli episodi), batch perse citabili, drill nel simulatore (resta roleplay in affiancamento).
- **2026-07-25 — Attivazione operatore: aha moment strumentato (leading indicator)**: primo pezzo del seguito allo studio onboarding (Nicholas: "parti dall'operatore"). Definizione v1 = "gap reale diagnosticato + allenato": l'operatore è ATTIVO quando entro una finestra (placeholder 7gg) vede un gap vero nel suo profilo-segnali (`/me/signals`, l'aha) e completa ≥2 scenari Academy nelle categorie che allenano quel gap (`recommendPathForGap(gap).categories`) sopra un floor qualità (overall ≥60). Engine PURO `src/lib/activation.js` (`computeActivation` unit-testabile, `ACTIVATION_THRESHOLDS` versionate `act-1-2026-07`; il copy UI legge le soglie da lì, mai hardcodate). 4 eventi non-fatali su hook esistenti: `signals_viewed` (`/api/me/signals`, + identità join-warehouse + baseline `SET nx`), `scenario_completed` (`/api/score`, dopo lo score_hist, con `categoryId` per il gap-match a read-time), `coaching_acknowledged` (`/api/me/coaching`), `turno_viewed` (`/api/me/turno`). Store KV isolato `activation:*` (eventi LIST cap 200 TTL 120gg, mai transcript, niente PII fan). Vista SEED `/admin/activation` (funnel aha→attivato, rate, latenza; empty-state onesto). **Decisioni chiave (con Nicholas)**: (1) è un LEADING indicator; la NORTH-STAR di validazione è il movimento reale del profilo-segnali warehouse — mai ottimizzare l'activation direttamente (trap-Airtable: es. "PPV su" = spam che brucia i fan); (2) la SOGLIA è placeholder, si DERIVA dai dati (magic-number) su coorte reale, non si copia; (3) gap-match a read-time (resta allineato al catalogo scenari), mai in hot path. **Vincoli onesti**: operatori non ancora onboardati → activation rate non calcolabile oggi (strumentato ora, letto dopo); l'app non vede il turno vero → loop-close INFERITO dal ricalcolo mensile dei segnali (proxy); solo turni singoli (duo non attribuibile → invisibili); dipende dal gate `COPILOT_PILOT` aperto agli operatori; premessa NON validata: lo score dello scenario correla col comportamento sul turno vero (prima validazione forward da fare). È il Kirkpatrick L3-4 applicato all'onboarding stesso (cfr studio onboarding, memory `onboarding-evidence-fieldguide`). Aperti: derivazione soglia, validazione transfer, guardia anti-Airtable con proxy retention fan, copertura duo via export Infloww. Branch `feature/operator-activation`.
- **2026-07-25 — Academy verso l'esterno: assessment candidati pre-assunzione (V1 segnale, V2 gated)**: il simulatore Academy come test pre-hire per i chatter. Motore estratto in `src/lib/academy-engine.js` (`generateFanReply`, `evaluateScenarioTranscript`) e CONDIVISO tra le route operatore (`/api/chat`, `/api/score`, refactor behavior-preserving) e le nuove route candidato — una sola fonte di scoring, niente drift (governance formula). Candidato = NON utente Clerk: route pubbliche self-guarding `GET/POST /api/candidate/[token]`, `…/chat`, `…/score` + pagina `/assessment/[token]` (bare route in `AppShell`, nessun chrome interno); il **token monouso** è l'auth (namespace KV `candidate:*` isolato da `session/score_hist/profile`/leghe/denaro). Data model `src/lib/candidate-assessments.js` (`ASSESSMENT_SUITES` = scenari reali, incluso un `compliance-*` come riga rossa; TTL 14gg; sequenza forzata anti-replay; consenso registrato). Pannello admin `/admin/candidate-assessments` (SEED): crea link, legge il report, registra l'esito. **Decisione chiave (Nicholas): V1 = SEGNALE human-in-the-loop (report a HR, non gate), V2 = gate automatico NON costruito.** Vincoli: GDPR art.22 (no decisione unicamente automatizzata), EU AI Act Annex III.4 (alto rischio, obblighi pieni 2 dic 2027), Italia Decreto Trasparenza 104/2022 + L.132/2025 (informativa attiva OGGI → consenso in-app), Garante già attivo su recruiting black-box. Unico filtro automatico legittimo = righe rosse compliance (violazione di regola, non punteggio). Merito: work-sample test ha validità reale ma modesta (r≈0.33) → affianca il colloquio, non lo sostituisce. **Bridge V2 deliberato**: campo `outcome{decision, employeeId}` aggancia il candidato all'employee una volta assunto → un domani correla score↔resa reale (operator-signals/shift-quality). Trigger V2 (tutti e 3): correlazione misurata su campione adeguato + DPIA/logging AI Act + revisione umana genuina anche sugli scartati. ADR completo: `docs/ADR-candidate-assessment.md`. Suite creator-agnostica in V1 (fairness); calibrazione per-creator/difficoltà non costruita.
- **2026-07-25 — Game film operatore: vinte da studiare, perse da correggere, gemella "come potevi giocarla"**: drill-down formativo del profilo-segnali (`/admin/operator-signals/[operator]`, SEED, link "game film →" dalla card operatore). Core PURO `src/lib/game-film-core.js` (classify/twins/trim/ladder, zero import, 30 asserzioni node) con soglie CALIBRATE su dati reali e VERSIONATE (`film-1-2026-07`: VINTA = fan ≥$100 in finestra turno +2h coda; PERSA = fan ingaggiato ≥15 msg uscito a $0 con PPV assente o mai ≥$40; MIN_OP_MSGS 3; il copy UI legge le soglie da `FILM_THRESHOLDS`, mai hardcodate) + orchestrazione `src/lib/operator-game-film.js` (stessa CTE turni-singoli di operator-signals; transcript batched con pruning; **paginazione pageToken aggiunta a `bqQuery` + parser esteso a REPEATED/RECORD** — prima le righe oltre ~10MB si perdevano in silenzio e ARRAY<STRUCT> arrivava in formato wire grezzo; cache KV 6h keyata sul nome GREZZO encodato — la normalizzazione non è iniettiva, due nomi distinti collassavano sulla stessa entry: lezione duo-coverage). Ogni persa è accoppiata alla vinta GEMELLA dello stesso operatore (stesso creator prima, poi ingaggio più vicino; fallback cross-creator DICHIARATO in card con badge "creator diverso"). Disciplina di onestà: chi propone offerte vere senza chiudere NON è persa (si valuta il gioco, non l'esito); coda +2h indicativa mai contabile, doppio-conteggio tra turni adiacenti <2h dichiarato nel footer; NESSUNA stima del $ perso (controfattuale non misurabile, by design); hedge esplicito "una persa può avere ragioni legittime: leggi la conversazione prima di farne coaching — il film propone, il coach decide"; ladder PPV dal transcript PIENO pre-trim (la card non può contraddire la propria reason); solo turni singoli (limite duo dichiarato). PII: fan pseudonimizzato con la `fanAlias` HMAC condivisa coi tape E `key`/`twin_key` derivate dall'alias, NON dallo user_id grezzo (l'invariante "user_id mai nel payload" è nel codice, non solo nei commenti); transcript con testo fan → SOLO superficie SEED, publish operatore-facing = curatela game tape (gate PII), non costruito in v1. Coaching, non score/comp. Validato E2E su Simone Cherchi (3.401 conversazioni → 30 vinte / 20 perse; la top persa: fan che scrive 55 volte, ZERO PPV proposti, nello stesso periodo delle masterclass da $1.8k). Review 4 lenti + capo revisore: 7 difetti alta/media confermati e risolti pre-merge (leak user_id nelle key, collisione cache normName, gemella cross-creator muta, ladder su transcript troncato, pageToken mancante, doppio-conteggio coda, framing mai_proposto); SQL injection esclusa (sqlStr: control-chars→backslash→apici, ordine corretto). Aperti: dedup contabile del doppio-conteggio, registro del giudizio coach ("persa legittima" persistente), wiring operatore-facing.
- **2026-07-25 — Onboarding role-funnel (/guida)**: engine `src/lib/role-funnels.js` (4 percorsi operatore/manager/leadership/people come DATI puri; `selectFunnels` per capability/scope, robusta ai ruoli custom) reso come percorso guidato in `/guida` (tab per ruolo, componente `RoleFunnelGuide`) + modale primo-accesso (`OnboardingNudge`, localStorage, montata in AppShell). Si insegna il funnel (sequenza+perché+decisione), non i bottoni; un engine, N funnel come contenuto. Nav 'Guida strumenti' in Sidebar+hub. Aperto: 'HR' non è ruolo RBAC → funnel People su scope all/admin, da agganciare a `selectFunnels` quando gli HR avranno un ruolo dedicato. PR #82.
- **2026-07-24 — Copertura duo nel profilo-segnali: due fonti affiancate, non fuse**: `src/lib/operator-duo-coverage.js` (`buildDuoCoverage`, v `duo-cov-1`) congiunge a READ-TIME i segnali dell'export Infloww (turni singoli+duo, #77) al profilo warehouse (solo singoli, `operator-signals.js`) sul SINGOLO operatore, chiudendo il buco orfano "in duo il warehouse non sa chi ha scritto" (lo store `infloww:opsig:*` era letto solo dalla sua superficie di ingest). Enrichment nella sola route SEED `operator-signals` (admin-first, NON wired a /me), stesso pattern di `attachCoachingPaths`. Disciplina (Nicholas): i numeri NON si incatenano né si fondono — `question_rate` (corr cross-fonte 0.78) guida il flag, `avg_ppv_price` (0.63) resta informativo, ciascuno dalla sua fonte; la differenza singolo↔tutti-i-turni è INDICATIVA dei duo, non contabile (finestre/definizioni diverse dichiarate a schermo + periodo per card). Guardie di onestà: identità ambigua (due nomi distinti che normalizzano uguale, su ENTRAMBI i lati wh/export) NON abbinata (un buco è meglio del numero di un altro sull'operatore sbagliato); export stantio (`period.to` >45gg) o senza periodo estraibile → flag depotenziato (non rosso) e FUORI dal conteggio `diverging` (`now` iniettato → deterministico/testabile). Fa emergere anche gli operatori visibili SOLO da export (mai abbastanza turni singoli → invisibili al profilo). Aggregati, niente PII fan, coaching non score. Lib unit-testato (62 asserzioni node), review adversariale multi-lente (1 finding LOW-MED "overclaim causale" + LOW risolte, verdetto merge-ready). Gate/non costruito: wiring operatore-facing (/me), match sulle conversazioni INTERE del warehouse (revenue/conversazione), automazione produzione export.
- **2026-07-24 — Academy ancorata ai dati reali, Tier 1: game tape**: superficie didattica che estrae dal warehouse (`onlyfans.chat` + `attributed_transactions` + finestre turno da `cache_members`/`loadShiftsForDay`) le migliori SEQUENZE di vendita reali per creator, le cura (admin SEED) e le pubblica agli operatori (`/academy/tapes`, auth-only). Lib `src/lib/academy-tapes.js` (extract/curate/list/stripTape), curatela `/admin/academy-tapes` (SEED). Pattern "call library" (Gong) + Evidence-Based Training (aviazione). Decisioni di design: (1) id tape ancorato a `seq.first` (stabile alla ri-estrazione → upsert idempotente, la curatela non si orfana); (2) **pseudonimizzazione via HMAC keyed** (segreto server `ACADEMY_TAPE_SECRET`, fallback `KV_REST_API_TOKEN`) — NON hash nudo di input enumerabili, che sarebbe reversibile; `user_id` esce solo dalla superficie SEED, `stripTape` lo toglie all'operatore; (3) attribuzione operatore onesta singolo/duo/nessuno come `shift-quality.js`; (4) la revisione pre-publish è anche gate PII sul testo libero; (5) i tape sono materiale didattico, FUORI da score/comp (policy dati-fan). Validato E2E su Elisa Esposito (12 tape da 531 sequenze). Tier 2 (smart trackers calibrati su outcome) VALIDATO empiricamente ma non ancora costruito: su ~4.950 turni singoli, prezzo PPV/cadenza correlano col revenue/ora (within-creator, 31/32 creator), question rate NEGATIVO (opposto di Gong → le regole si ricalibrano, non si importano). Vedi memory `academy-real-data-grounding` / `academy-tier2-signals-evidence`.
- **2026-07-24 — Copertura duo via export Infloww: schema + match VALIDATI**: il warehouse non attribuisce l'operatore nei turni in duo (`sender_id`=account creator); l'export Message Dashboard di Infloww sì (colonna `Sender`). Primo export reale ingerito → schema colonne chiuso (`docs/INFLOWW_SURFACE.md` §24 lug: 12 col, `Sent to` porta il `user_id` fan, `Creator Message` in HTML, orari UTC+1). Match export↔warehouse VALIDATO su dati reali: 36/40 per user_id+testo, prezzo PPV 36/36, offset costante +1h. Cross-validazione segnali Infloww vs warehouse su 119 operatori: **tasso domande corr 0.78, prezzo PPV 0.63** (il non-1.0 è atteso: Infloww copre tutti i turni, warehouse solo i singoli → chi diverge è chi cambia in duo, il valore aggiunto). Engine `src/lib/infloww-signals.js` (`computeInflowwOperatorSignals`, unit-testato): dà i segnali COUNT-based affidabili (tasso domande, prezzo medio PPV, quota PPV); la **cadenza-per-ora NON è derivabile** da questo export (mancano le ore → Clocked Hours o finestre turno). **VINCOLO chiave (Nicholas): l'export ha un TETTO ~500k messaggi** → "tutti gli operatori" cattura solo ~5 giorni; per un mese vero di una persona serve l'export SCOPED per-operatore (file piccolo). Superficie di UPLOAD COSTRUITA (`/admin/infloww-ingest`, SEED, `src/lib/infloww-ingest.js`): il client parsa l'xlsx/csv con SheetJS + calcola i segnali NEL BROWSER (i messaggi grezzi NON escono, solo aggregati al server — privacy) → upsert per operatore (latest-wins, la copertura cresce). Pipeline verificata sul file reale (stessi numeri del calcolo Python). NON costruito: automazione produzione export via computer-use (feeder, board-gated su "quanto è penoso l'export a mano") + batch del file org intero + match al warehouse per revenue/conversazione intera.
- **2026-07-24 — Coaching su misura, passo 2: gap → percorso di training**: `src/lib/coaching-paths.js` (`recommendPathForGap`, `attachCoachingPaths`) traduce il `top_gap` del profilo-segnali nel percorso concreto — quali scenari Academy allenano quel comportamento, sulla stessa tassonomia di categorie di `coaching-center.js` (round-robin tra le 2 categorie mappate per dare priorità alla più rilevante ma includere la secondaria). Enrichment a READ-TIME nella route `operator-signals` (non in cache: resta allineato al catalogo scenari corrente). Deterministico, unit-testato (4 gap → 3 scenari su entrambe le categorie). Complementare alla Coaching Center (che resta la sede delle assegnazioni formali per periodo/CP). Il simulatore NON ha deep-link a scenario → si linka `/`. Review adversariale pulita (0 difetti). Passo 3 (verifica transfer reale) non costruito.
- **2026-07-24 — Metodo × Resa: skill vs outcome per operatore (confronto, non fusione)**: estende `operator-signals.js` (v `op-sig-3`) con la RESA aggiustata per creator affiancata al METODO comportamentale. Regola di disciplina (concordata con Nicholas): i match/aggregati non si INCATENANO — ogni numero da UNA fonte; il revenue per operatore viene dal binario autorevole `cache_members.amount` (attribuzione Infloww/CP), NON da un match fuzzy. `rev_index` = venduto reale ÷ atteso al rate dei PARI sugli stessi creator, con baseline **leave-one-out** (esclude l'operatore stesso: senza, chi domina un creator prende un falso 1.00 "+0% vs pari") e floor di copertura (cmp_h ≥ 50% ore, altrimenti null). `alignment` = media goodness dei comportamenti (prezzo PPV nullo = "non monetizza mai" conta come peggiore, non scartato). Quadrante metodo×resa: star / potenziale (abitudini ok, resa sotto → contesto) / fragile (rende senza metodo) / da-coachare; le diagonali opposte sono il valore diagnostico (separa skill da fortuna-del-creator). Solo turni singoli. Coaching, non score. Review adversariale: 6 finding (1 med self-inclusion + 5 low) risolte. Riconciliato col parallelo #78 (`slow_reply_rate`): additivi, `op-sig-3` copre entrambi.
- **2026-07-24 — Coaching su misura, fondazione: profilo-segnali per operatore**: `src/lib/operator-signals.js` (`getOperatorSignalProfiles`, versione `OPERATOR_SIGNALS_VERSION`, cache KV riscaldata dal cron dispatch) porta i Signals org-level (`academy-signals`) alla grana del SINGOLO OPERATORE: dai suoi turni a operatore singolo legge i comportamenti reali (tasso domande, prezzo/cadenza PPV, cadenza msg — la chat reale ha i prezzi, a differenza del simulatore) e li benchmarka contro la distribuzione org (percentile → verdetto forte/ok/gap), flaggando il "gap" principale (dove è carente) = seme del coaching su misura. Superficie `/admin/operator-signals` (SEED). Validato su dati reali (148 operatori profilati; es. chi presidia bene ma sotto-prezza i PPV). Review adversariale: 4 finding LOW risolte (empty-state, i18n, caveat prezzo visibile; nota governance: SEED etichettata "demo" ma gatea PII/denaro → task separato). **COPERTURA: solo turni singoli; i turni in duo NON sono attribuibili dal warehouse (chat non porta l'umano).** La copertura duo si estende con lo strumento di MATCH dell'export Infloww Message Dashboard (per-sender, ha l'employeeId) sulle conversazioni intere del warehouse: costruibile e validabile su ground-truth sintetico, MA lo schema colonne esatto del CSV export non è mai stato catturato (`docs/INFLOWW_SURFACE.md`: "si chiude col primo export reale") → serve un export campione prima di costruire il parser. Vision completa (Nicholas, lug 2026): profilo per-operatore → percorso di training su misura → verifica del transfer reale (migliori sul vivo o solo sul simulatore?) — è il "Kirkpatrick livello 3-4" che pochi possono fare perché servono i dati del campo, che noi abbiamo.
- **2026-07-24 — Academy Tier 3: signal-scoring del simulatore (additivo)**: `src/lib/academy-signal-scoring.js` (`scoreTranscriptSignals`, versione `SIGNAL_SCORING_VERSION`) calcola in modo DETERMINISTICO quanto un transcript di training si allinea ai comportamenti che monetizzano (evidenza Tier 2). Vincolo di design: il simulatore manda solo `{role, content}` (niente prezzi/tempi) → l'unico segnale validato misurabile sul testo è il TASSO DI DOMANDE (negativo); prezzo/cadenza PPV sono dichiarati "si allena sul vivo", non finti; lunghezza msg e talk ratio mostrati come "nessun segnale" per non alimentare miti. **ADDITIVO by design**: `score.signals` è calcolato DOPO overall/xp/stars in `/api/score`, in try/catch non-fatale, e NON tocca la ladder né `applyScoreToProfile` — la calibrazione dello score LLM sulla ladder (che sposterebbe le classifiche retroattivamente) resta un passo separato board-gated, con la governance versionata dello score CP. Superficie: componente `SignalsPanel` nel risultato del simulatore (`src/app/page.js`) e nella review `/admin/sessions/[id]`. Validato con unit test (conduce→100, intervista→0, corta→nessun dato, input nullo→robusto). Vedi memory `academy-tier2-signals-evidence`.
- **2026-07-24 — Academy Tier 2: Signals (smart trackers calibrati su outcome)**: superficie READ-ONLY admin (`/admin/academy-signals`, SEED) che ricalcola dal warehouse quali comportamenti operatore correlano col revenue/ora, sui turni a OPERATORE SINGOLO e DENTRO ogni creator (within-creator, media delle CORR per-creator). Lib `src/lib/academy-signals.js` (versione `SIGNALS_VERSION`, cache KV `academy:signals:{version}` riscaldata dal cron dispatch, TTL 25h). Risultato su ~4.3k turni singoli/30 creator (lug 2026): prezzo PPV +0.41 e cadenza PPV +0.30 (forti, 29/30 creator concordano), cadenza msg +0.24, **tasso domande −0.10** (opposto del B2B di Gong → i signal si calibrano sui nostri dati, non si importano); talk-ratio e lunghezza msg = nessun segnale. È il "Gong Labs interno": informa il coaching, NON entra in score/comp. Lezioni dalla review (bug corretti prima del merge): (1) grana per-SHIFT, mai `GROUP BY (creator,ore,revenue)` — fonde turni distinti e gonfia la cadenza; (2) il test "turno singolo" (NOT EXISTS di sovrapposizioni) va fatto contro TUTTI i turni sul creator (CTE `all_shifts` senza filtro ore/ruolo), non contro i soli chatter filtrati, altrimenti un turno duo passa per singolo; (3) floor `MIN_PAIRS` sulle coppie non-null per creare la CORR di un creator (i comportamenti sparsi come il prezzo PPV saturano a ±1 su pochi punti). Vedi memory `academy-tier2-signals-evidence`. Prossimo (non costruito): scoring del simulatore calibrato su questi signal, con la governance versionata dello score CP.
- **2026-07-20 — Fix cron mai scattati + sync ledger automatico**: i 4 cron di vercel.json prendevano 401 dal middleware Clerk (path non pubblici — stessa classe del bug ingest lug 2026): MAI eseguiti in produzione, verificato con curl. Fix: path cron pubblici nel middleware + auth centralizzata in `src/lib/cron-auth.js` (con `CRON_SECRET` configurato accetta SOLO il Bearer — `x-vercel-cron` da solo è spoofabile su route pubblica; senza secret, transizione su header). Nuovo `/api/cron/payout-ledger`: tick auto-concatenante (vincolo Hobby: cron max giornalieri) che avanza il job ledger o avvia il refresh del primo periodo stantio tra mese corrente e precedente (>20h); stessa meccanica per `/api/cron/cp-wages` (wage CP). **Su Hobby i cron scattano "entro l'ora" (±59 min, doc Vercel) → l'ordine tra cron separati NON è garantito** (il digest 06:05 può precedere il run 06:00 slittato) e una finestra può andare persa senza traccia (osservato 20/07: snapshot 00:05 scattato alle 00:10 — primo cron reale della storia del progetto — run alert 06:00 mai, probabile interferenza del deploy in mezzo alla finestra). Il limite di conteggio NON c'entra (100 cron/progetto su tutti i piani dal 20 gen 2026). → vercel.json dichiara SOLO 2 cron: snapshot leaderboard (00:05 lun) + `/api/cron/dispatch` (03:00 UTC giornaliero) che esegue il resto in sequenza deterministica (lun: alert run POI digest; giorno 1: snapshot leghe; sempre: tick cp-wages e payout-ledger). Heartbeat `cron:heartbeat:*` a ogni tick per la prova di esecuzione. Regole: mai aggiungere un path a isPublicRoute senza difesa propria nella route; i cron nuovi vanno nel dispatcher, non come 3º cron in vercel.json.
- **2026-07-19 — Trasparenza comp v2: albero payout fan-level (ledger persistito)**: le transazioni Infloww (`/v1/transactions`, con `transactionId` stabile + refund joinabili — schema verificato live, vedi `docs/INFLOWW_SURFACE.md` §19 lug) vengono sincronizzate in KV per creator×mese (`payout-ledger.js`) e abbinate ai take CP per finestra turno + importo lordo pieno O quota coseller (`payout-match.js` — nei turni a k coseller CP splitta la vendita: take = gross/k, calibrato su dati reali; confidenza esplicita esatto/quota/ambiguo/non-abbinato — il payload API non ha l'employee, l'attribuzione è inferita e dichiarata DIREZIONALE, mai contabile). Misurato su lug 2026: copertura 90%+ sul top operatore; refund report → ~$1.3k di comp stimata su vendite rimborsate in 19 giorni (≈11% del rimborsato attribuito, coerente con gli scaglioni). Scelto ledger persistito (non live) perché la dispute policy richiede evidenza congelata e il rate limit è condiviso. Superficie: `/admin/payout-tree` (albero + refund impact report, `authorizeAll(SCORES_VIEW)`; sync SEED). Fase v2.1 (albero in `/me/compenso`, fan pseudonimizzati) è GATED sul match-rate reale misurato in v2.0 (proposta ≥85%) — non costruirla prima. Dati fan restano fuori dagli input dello score by design.
- **2026-07-19 — Roadmap di prodotto in-app**: le idee/feature future vivono in `/admin/roadmap` (KV `roadmap:items`, admin-only, colonne in corso/prossime/più avanti/parcheggiate con gate esplicito) invece che come pagine placeholder in nav — lezione Content Pipeline. Le nuove idee di sessione vanno aggiunte lì, non costruite come scaffolding. Studio traffic automation (OnlyFlow) in `docs/TRAFFIC_AUTOMATION_STUDY.md`, parcheggiato dietro gate board su risk appetite.
- **2026-07-18 — Governance formula score + gate benchmark chiusi**: snapshot della formula a ogni import (`/admin/score-config-history`) e bozze con backtest sui mesi reali + publish confermato (`/admin/score-config-drafts`); dossier benchmark in `docs/BENCHMARK_DEEP_STUDY.md`, superficie Infloww in `docs/INFLOWW_SURFACE.md` (API ufficiale senza endpoint transcript → fonte chat = export Message Dashboard; chiave API in env è placeholder, da sostituire), policy dispute in `docs/CAREER_LADDER.md` §8.2, requisiti legali in `docs/LEGAL_SCORING_REQUIREMENTS.md`. PR #27-#30.
- **2026-07-17 — Alert operativi (findings store)**: motore di check deterministici su cron → KV `ops:alerts:*` (fingerprint dedup, stati open/ack/resolved, auto-resolve), pagina `/admin/alerts` + pannello hub top-3 + badge critici in sidebar. Pattern issue-tracker (Sentry/Datadog), non notification feed. ADR completo in `docs/ALERT_OPERATIVI.md`. Digest email del lunedì via Resend (`/api/admin/ops-alerts/digest`, solo se critici aperti). Layer LLM escluso dalla v1 by design.
- **2026-07-13 — Gating dati denaro pre-CM**: helper `authorizeAll` (scope "all" obbligatorio) su 7 route leaderboard denaro + `closed-loop-metrics` → ANALYTICS_VIEW, prima dell'onboarding CM pilota (ruolo team_lead, cockpit `/cm-cockpit`). Motivo: le route esponevano venduto/score di tutti a qualsiasi utente loggato. PR #24.
- **2026-07-13 — Career ladder + Cockpit CM**: `docs/CAREER_LADDER.md` (Fase 0 people lifecycle) è l'artefatto sorgente di livelli/gate/comp; cockpit CM live (PR #22/#23) con tracciamento supervisioni (KV `cm:sup:*`), override 3% in shadow mode, capability `cm.cockpit`.
- **2026-07-10 — Potatura superficie**: rimosse 14 route/lib morte (probe CP/Infloww, `/api/sessions`, `/api/feedback`, `/api/challenge`, client CP duplicato), AdminNav stub, Content Pipeline fuori da nav/hub/welcome/cron, COLORS legacy rimappata sui token Dark SaaS, TIER flat. Motivo: 3 generazioni di tool sedimentate rendevano l'app illeggibile per un nuovo utente. Reversibile via git.

## Non fare

- ❌ Mai modificare l'auth Clerk senza un piano (bypass parziali rompono 20 route)
- ❌ Mai cambiare il calcolo dello Score senza prima salvare in `leaderboard-config.js` / `creatorspro-score.js` e versionare il commento (gli operatori vedono lo storico cambiare)
- ❌ Mai esporre il KV in route pubbliche senza `authorize()`
- ❌ Mai aggiungere npm packages pesanti (>500KB minified) senza dynamic import o segnalazione esplicita
- ❌ Mai rimettere in navigazione la Content Pipeline finché le API rispondono 501
- ❌ Mai reintrodurre route "probe" usa-e-getta committate: si esplorano le API esterne in locale/scratch, non in produzione
