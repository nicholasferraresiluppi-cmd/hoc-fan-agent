"use client";

// Card di un draft in lista (per pagina queue).

export default function DraftCard(/* { draft, onClick } */) {
  // TODO: render status badge, body preview, creatorSlug, publishAt.
  return (
    <div className="border border-[#1B1E26] rounded p-3 bg-[#111318]">
      TODO: DraftCard
    </div>
  );
}
