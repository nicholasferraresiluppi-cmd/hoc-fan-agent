"use client";

// Bottoni approve/reject/edit per un draft.
// Approve apre un SchedulePicker inline o modale per scegliere publishAt.

export default function ApprovalActions(/* { draftId, onApprove, onReject } */) {
  // TODO: 3 bottoni + handler che chiamano /api/content-pipeline/approve|reject|queue/[id]
  return <div className="flex gap-2">TODO: ApprovalActions</div>;
}
