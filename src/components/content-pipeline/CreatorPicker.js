"use client";

// Dropdown selettore creator (per filtri queue/history).

export default function CreatorPicker(/* { value, onChange, includeAll = true } */) {
  // TODO: SWR su /api/content-pipeline/creators + <select>
  return <div className="text-[#B9BDC7]">TODO: CreatorPicker</div>;
}
