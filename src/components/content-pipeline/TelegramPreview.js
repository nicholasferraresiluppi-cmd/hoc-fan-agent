"use client";

// Preview stilizzato di un post Telegram (mimica light theme della chat).

export default function TelegramPreview(/* { body, mediaUrls } */) {
  // TODO: renderizzare body con parse_mode HTML safe + thumbnail media.
  return (
    <div className="rounded-2xl bg-[#111318] border border-[#1B1E26] p-4 max-w-md">
      TODO: TelegramPreview
    </div>
  );
}
