"use client";

/**
 * AdminNav — DEPRECATED stub.
 *
 * La navigazione admin è stata sostituita dalla Sidebar globale CP-style
 * renderizzata da <AppShell> nel root layout (src/app/layout.js).
 *
 * Questo componente è mantenuto come stub no-op per non rompere gli import
 * esistenti nelle pagine admin durante la migrazione. Renderizza `null`.
 *
 * Da rimuovere quando tutti gli import saranno stati ripuliti.
 */
export default function AdminNav() {
  return null;
}
